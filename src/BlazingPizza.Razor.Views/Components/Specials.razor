<div class="main">
    <ul class="pizza-cards">
        @if(ViewModel.Specials is not null)
        {
            @foreach(PizzaSpecial special in ViewModel.Specials)
            {
                <li style="background-image:url('https://ticapacitacion.blob.core.windows.net/blazingpizzaimages/@special.ImageUrl')">
                    <div class="pizza-info">
                        <span class="title">@special.Name</span>
                        @special.Description
                        <span class="price">
                            @special.GetFormatedPrice()
                        </span>
                    </div>
                </li>
            }
        }
        else
        {
            <li>Loading ...</li>
        }
    </ul>
</div>